<!DOCTYPE html>
<html>
  <head>
    <%-include('../common/head_html.html') %>
  </head>
  <body>
    <%-include('../common/nav.html') %>
    <br />

    <div class="container my-3">
      <div class="d-flex justify-content-end">
        <div class="btn-group" role="group" aria-label="Basic outlined example">
          <button
            id="detail_deleteButton"
            type="button"
            class="btn btn-outline-primary"
          >
            삭제
          </button>
        </div>
      </div>
      <h2 id="detail_title" class="border-bottom py-2">제목 :</h2>
      <div class="d-flex justify-content-end">
        <p id="detail_name" class="mx-2">작성자 :</p>
      </div>
      <div class="d-flex justify-content-end">
        <p id="detail_createdAt">연락처 :</p>
      </div>
      <div class="d-flex justify-content-end">
        <p id="detail_createdAt">작성날짜 :</p>
      </div>
      <%-include('./imgCarousel.html') %>
      <h5 id="detail_title" class="border-bottom py-2">가격 :</h5>
      <div class="card my-3">
        <div class="card-body">
          <div
            id="detail_knowhow"
            class="card-text"
            style="white-space: pre-line"
          >
            글 :
          </div>
        </div>
      </div>
    </div>

    <%-include('../common/basicScript.html') %>
  </body>
</html>
