<!DOCTYPE html>
<html>
  <head>
    <%-include('../common/head_html.html') %>
  </head>
  <body>
    <%-include('../common/nav.html') %>
    <br />

    <div class="container my-3">
      <div class="d-flex justify-content-end">
        <div class="btn-group" role="group" aria-label="Basic outlined example">
          <div class="mx-3 py-1">
            <button id="heart-red" class="mx-3 btn position-relative hidden">
              <i class="fa fa-heart" style="font-size: 28px; color: red"></i>
              <span
                class="
                  position-absolute
                  top-0
                  start-100
                  translate-middle
                  badge
                  rounded-pill
                  bg-danger
                  heartCntSpan
                "
                id="redHeartCnt"
              >
                99
              </span>
            </button>
            <button id="heart-white" class="mx-3 btn position-relative hidden">
              <i class="fa fa-heart-o" style="font-size: 28px"></i>
              <span
                class="
                  position-absolute
                  top-0
                  start-100
                  translate-middle
                  badge
                  rounded-pill
                  bg-danger
                "
                id="whiteHeartCnt"
              >
                99
              </span>
            </button>
          </div>
          <button
            id="deatail_updateButton"
            type="button"
            class="btn btn-outline-primary"
          >
            수정
          </button>
          <button
            id="detail_deleteButton"
            type="button"
            class="btn btn-outline-primary"
          >
            삭제
          </button>
        </div>
      </div>
      <h2 id="detail_title" class="border-bottom py-2">제목 :</h2>
      <div class="d-flex justify-content-end">
        <p id="detail_name" class="mx-2">작성자 :</p>
      </div>
      <div class="d-flex justify-content-end">
        <p id="detail_createdAt">작성날짜 :</p>
      </div>
      <h5 id="detail_address" class="border-bottom my-3 py-2">주소 :</h5>
      <h5 id="detail_campsite" class="my-3 py-2">시설종류 :</h5>

      <!-- 유료캠핑장 정보 아코디언 -->
      <div class="accordion accordion-flush" id="accordionFlush"></div>

      <div class="card my-3">
        <h5 class="card-header">후기</h5>
        <div class="card-body" id="description_body">
          <p class="card-text" id="detail_description"></p>
        </div>
      </div>

      <%-include('./imgCarousel.html') %>
    </div>

    <%-include('../common/basicScript.html') %>
    <script src="/js/story/storyDetail.js"></script>
    <script>
      redirectLoginForUnauthorization();
      const storyId = <%=storyId%>
      getHeartState(storyId)
      makeDetailStory(storyId)
      activateHeartButton(storyId)
    </script>
  </body>
</html>
